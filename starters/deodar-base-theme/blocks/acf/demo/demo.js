import $ from 'jquery';
import gsap from 'gsap';

// Class 
class Demo{
    constructor($root){}
}

$( function(){
    // Maybe iterate over each of these to support multiple on a page. 
    new Demo($(".demo"));
} );
